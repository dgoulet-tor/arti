[workspace]

# Here we declare that this is a workspace, containing several crates
# within it.
members = [
    "caret",
    "tor-llcrypto",
    "tor-protover",
    "tor-bytes",
    "tor-checkable",
    "tor-cert",
    "tor-proto",
    "tor-netdoc",
    "tor-netdir",
    "client-demo",
    ]